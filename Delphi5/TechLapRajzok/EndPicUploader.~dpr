program EndPicUploader;

uses
  Forms,
  Unit1 in 'Unit1.pas' {EndPicUploaderForm};

{$R *.RES}

begin
  Application.Initialize;
  Application.CreateForm(TEndPicUploaderForm, EndPicUploaderForm);
  Application.Run;
end.
